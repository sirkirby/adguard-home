! Title: Custom one-off AdGuard DNS Allow List
! Description: General allow list for one-off sites
! Last modified: 2026-08-02 16:21:54
! Homepage: https://raw.githubusercontent.com/sirkirby/adguard-home/main/custom-allow.txt

! Feature flags and remote configuration
! LaunchDarkly flag APIs; events.launchdarkly.com intentionally remains blockable.
@@||clientsdk.launchdarkly.com^ # LaunchDarkly client flag polling
@@||clientstream.launchdarkly.com^ # LaunchDarkly client flag streaming
@@||sdk.launchdarkly.com^ # LaunchDarkly server flag polling

! Split flag delivery; events.split.io and telemetry.split.io intentionally remain blockable.
@@||sdk.split.io^ # Split targeting rules
@@||auth.split.io^ # Split streaming authentication
@@||streaming.split.io^ # Split realtime flag updates
@@||cdn.split.io^ # Split flag configuration CDN

@@||firebaseremoteconfig.googleapis.com^ # Firebase Remote Config

! ConfigCat SDKs only download configuration; the management API and dashboard are not allowed here.
@@||cdn.configcat.com^ # ConfigCat legacy/default config CDN
@@||cdn-global.configcat.com^ # ConfigCat global config CDN
@@||cdn-eu.configcat.com^ # ConfigCat EU config CDN

@@||config.ff.harness.io^ # Harness flag evaluations; events.ff.harness.io remains blockable
@@||cdn.growthbook.io^ # GrowthBook feature configuration CDN

! DevCycle flag evaluation/config; events.devcycle.com intentionally remains blockable.
@@||config-cdn.devcycle.com^ # DevCycle local-bucketing configuration
@@||sdk-api.devcycle.com^ # DevCycle client SDK flag evaluation and updates
@@||bucketing-api.devcycle.com^ # DevCycle cloud flag evaluation

! CloudBees Feature Management / Rollout legacy configuration endpoints.
! analytic.rollout.io, x-api.rollout.io, and Bugsnag intentionally remain blockable.
@@||conf.rollout.io^ # CloudBees flag configuration
@@||statestore.rollout.io^ # CloudBees flag state
@@||push.rollout.io^ # CloudBees realtime configuration updates

! Optimizely configuration CDNs are already allowlisted in media-allow.txt.
! PostHog, Statsig, Flagsmith, Eppo, Unleash, and Kameleoon use mixed-use or deployment-specific hosts.
@@||us.i.posthog.com^ # Wispr Flow flags; this host also carries PostHog telemetry

@@||yogaphysics\.com  # yoga
@@||embed.acuityscheduling.com^ # Added from Query Log
@@||statsig.anthropic.com^ # claude refused to login without tracking
@@||auth.openai.com^$important
@@||stifx8nc9g.cloudflare-gateway.com^$important
